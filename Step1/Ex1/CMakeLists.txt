cmake_minimum_required(VERSION 3.18)
    
# here we specify that the project is C language only, so the default
# C compiler on the system will be used
project(Ex1 VERSION 0.0.1)

add_executable(main main.c)
